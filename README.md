# MadeMyToys - Progetto WITUP

Questo progetto è stato generato automaticamente utilizzando WITUP GENESIS - Deploy Kit.

## Struttura del progetto

- `src/web/`: Applicazione web frontend
- `src/mobile/`: Applicazione mobile
- `.spec/`: Specifiche e template per lo sviluppo
- `_deploy_kit/`: Kit di installazione originale

## File di configurazione

- `.clauderules`: Regole per l'agente Claude
- `.clauderules-ux`: Regole specializzate per UX/UI
- `BLUEPRINT_TEMPLATE.md`: Template architetturale master
- `GENESIS_PROMPT.txt`: Prompt di attivazione per l'installer AI

## Primi passi

1. Consulta `README_INSTALL.md` per le istruzioni di installazione
2. Segui le indicazioni in `GENESIS_PROMPT.txt` per configurare l'ambiente
3. Utilizza i template in `.spec/` per iniziare lo sviluppo

## Sviluppo

### Web
```bash
cd src/web
# Comandi per avviare lo sviluppo web
```

### Mobile
```bash
cd src/mobile
# Comandi per avviare lo sviluppo mobile
```

## Licenza

Progetto generato con WITUP. Consulta i file di licenza per maggiori dettagli.